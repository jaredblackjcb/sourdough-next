"use client";
// Theme created using https://zenoo.github.io/mui-theme-creator/
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#00796b",
    },
    secondary: {
      main: "#1823b5",
    },
    info: {
      main: "#1823b5",
    },
  },
});

export default theme;
